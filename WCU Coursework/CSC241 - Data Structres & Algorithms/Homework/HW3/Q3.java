
numSiblings(Tree t, Node n)
{
    Queue q = new Queue();
    q.enqueue(n.value)  //Adding parent value to queue
    if (condition) {
        
    } (n.lchild !
    = null)
        q.enqueue(n.lchild.value) //Add lchild value to queue
    while ()
}